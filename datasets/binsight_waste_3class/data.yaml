# BinSight 3-class waste detection — YOLO Detect, bounding boxes only.
#
# `path` is relative to the REPOSITORY ROOT, which is where Ultralytics resolves
# a relative value from (verified against 8.4.9 — it uses the working directory,
# not this file's location). Portable: no machine-specific absolute path.
#
# Train from the repository root:
#   YOLO("yolo26n.pt").train(data="datasets/binsight_waste_3class/data.yaml")
#
# Generated by scripts/prepare_waste_dataset.py — do not hand-edit.

path: datasets/binsight_waste_3class
train: images/train
val: images/val
test: images/test

names:
  0: paper
  1: plastic
  2: metal
