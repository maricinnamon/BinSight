# BinSight — training environment
#
# Target: Python 3.12 (matches the Google Colab default runtime).
# On Colab, only keras-hub actually needs installing; everything else is
# already present at these versions. See ml/README.md for why each pin exists.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

tensorflow==2.19.0
keras>=3.8.0,<4.0.0
keras-hub==0.30.0

numpy>=2.0.2,<3.0.0
pandas>=2.2.0,<3.0.0
scikit-learn>=1.5.0,<2.0.0
matplotlib>=3.8.0,<4.0.0
pillow>=10.3.0,<12.0.0

# Optional: standalone LiteRT interpreter. Not required — tf.lite.Interpreter
# from the TensorFlow pin above is what the notebook uses for validation.
# ai-edge-litert>=1.0.0

# Optional and Python <= 3.11 ONLY: the TFLite Model Metadata writer.
# tflite-support 0.4.4 publishes no cp312 wheel, so this cannot install on a
# Python 3.12 runtime. The notebook detects that and falls back to
# labels.txt + model_contract.json. See ml/README.md § Metadata.
# tflite-support==0.4.4 ; python_version < "3.12"
